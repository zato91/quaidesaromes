<div class="container">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-3">
      <h2 class="text-center">L'équipe</h2>
      <%= simple_form_for [@wine,@game_user, @game_user_session], :url => update_pro_wine_game_user_game_user_session_path(@wine, @game_user, @game_user_session)do |f| %>
        <%= f.input :team_name, label: "Nom de votre equipe" %>
        <%= f.input :photo, as: :attachinary %>
      </br>
        <span class="flex padded-top padded-bot"><%= f.button :submit, "#{@submit_label}", class: "btn btn-primary" %></span>
      <% end %>
      </br>
      </br>
    </div>
  </div>
</div>

